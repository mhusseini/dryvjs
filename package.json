{
  "name": "dryv",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "publish": "yarn workspaces foreach --no-private -A npm publish"
  }
}
